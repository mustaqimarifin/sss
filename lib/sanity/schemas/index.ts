import author from './author';
import post from './post';
import tag from './tag';
import siteConfig from './siteConfig';
import blockContent from './blockContent';
const schemaTypes = [author, post, blockContent, tag, siteConfig];

export default schemaTypes;
